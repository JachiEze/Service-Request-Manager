@using Microsoft.AspNetCore.Mvc.Rendering
@{
    var currentController = ViewContext.RouteData.Values["controller"]?.ToString();
    var currentAction = ViewContext.RouteData.Values["action"]?.ToString();
}
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>@ViewData["Title"] - Service Request Management</title>
    <link rel="stylesheet" href="~/lib/bootstrap/dist/css/bootstrap.min.css" />
    <link rel="stylesheet" href="~/css/site.css" asp-append-version="true" />
    <link rel="stylesheet" href="~/ServiceRequestForm.styles.css" asp-append-version="true" />
</head>
<body>
    <header>
        <nav class="navbar navbar-expand-sm navbar-toggleable-sm navbar-light bg-white border-bottom box-shadow mb-3 fixed-top">
            <div class="container-fluid">
                <a class="navbar-brand" asp-area="" asp-controller="Home" asp-action="Index" style="font-weight:700; letter-spacing:1px;">
                    <span class="text-dark">Service Request Management Application</span>
                </a>
                <button class="navbar-toggler" type="button" data-bs-toggle="collapse" 
                        data-bs-target=".navbar-collapse" aria-controls="navbarSupportedContent" 
                        aria-expanded="false" aria-label="Toggle navigation">
                    <span class="navbar-toggler-icon"></span>
                </button>
                <div class="ms-auto d-flex align-items-center">
                    <img src="~/images/picture.jpg" alt="Logo" style="height:40px;" />
                </div>
            </div>
        </nav>
    </header>

    @if (currentAction == "Index" && (currentController == "User1" || currentController == "User2" || currentController == "User3"))
    {
          <nav class="sidebar text-white p-3 position-fixed top-0 start-0 h-100 shadow-lg" 
    style="width: 220px; margin-top: 56px; z-index: 1040; overflow-y: auto; background: linear-gradient(180deg, #0d1b4c 80%, #1e2a78 100%);">
            <div class="d-flex align-items-center mb-4">
                <span class="fs-4 me-2"><i class="bi bi-list"></i></span>
                <h4 class="mb-0">Menu</h4>
            </div>
            <ul class="nav flex-column">
                @if (currentController == "User1")
                {
                    <li class="nav-item mb-3">
                        <form asp-action="CreateForm" method="post">
                            <button type="submit" class="btn btn-link nav-link text-white text-start px-0">
                                <i class="bi bi-plus-circle me-2"></i> Request a New Form
                            </button>
                        </form>
                    </li>
                }
                <li class="nav-item mb-3">
                    <form asp-action="Grid" method="get">
                        <button type="submit" class="btn btn-link nav-link text-white text-start px-0">
                            <i class="bi bi-table me-2"></i> Grid Page
                        </button>
                    </form>
                </li>
                <li class="nav-item mb-3">
                    <form asp-controller="Account" asp-action="Logout" method="post">
                        <button type="submit" class="btn btn-link nav-link text-white text-start px-0">
                            <i class="bi bi-box-arrow-right me-2"></i> Logout
                        </button>
                    </form>
                </li>
            </ul>
        </nav>
        <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.5/font/bootstrap-icons.css">
    }

    <div class="content-area" style="margin-top:70px; margin-left:@((currentAction=="Index" && (currentController=="User1" || currentController=="User2" || currentController=="User3")) ? "220px" : "0"); padding: 0;">
    <main role="main" class="pb-3">
            @RenderBody()
        </main>
    </div>

    <footer class="border-top footer text-muted"></footer>

    <script src="~/lib/jquery/dist/jquery.min.js"></script>
    <script src="~/lib/bootstrap/dist/js/bootstrap.bundle.min.js"></script>
    <script src="~/js/site.js" asp-append-version="true"></script>
    @await RenderSectionAsync("Scripts", required: false)
</body>
</html>

<script>
    if ('scrollRestoration' in history) {
        history.scrollRestoration = 'manual';
        window.onload = function () {
            if (sessionStorage.scrollTop) {
                window.scrollTo(0, sessionStorage.scrollTop);
            }
        };
        window.onbeforeunload = function () {
            sessionStorage.scrollTop = window.scrollY;
        };
    }
</script>

